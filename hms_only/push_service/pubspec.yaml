name: push_service
description: "HMS-only push service"
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.3.4

dependencies:
  flutter:
    sdk: flutter

  push_service_hms:
    path: ../../mobile_service_component/push_service/push_service_hms